import { BeterHightlightDirective } from './beter-hightlight.directive';

describe('BeterHightlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BeterHightlightDirective();
    expect(directive).toBeTruthy();
  });
});
